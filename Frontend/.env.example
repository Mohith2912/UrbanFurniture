# Public HTTPS URL of the separately deployed Urban Furniture Flask API.
# The Next.js server proxies /api requests to this URL so browser sessions stay same-origin.
URBAN_BACKEND_URL=https://urban-furniture-api.example.vercel.app
